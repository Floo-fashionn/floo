const routers = require("express").Router();
// const ProductController = require("../controllers/productController");

// routers.get("/", ProductController.showAll);
// routers.get('/:id', ProductController.showById)
// routers.post('/', upload.single('imgUrl'), ImageKit, ProductController.createProduct)
// routers.put('/:id', authorization, upload.single('imgUrl'), ImageKit,ProductController.updateProduct)
// routers.delete('/:id', authorization, ProductController.deleteProduct)
// routers.patch('/:id', isAdmin, ProductController.updateStatusProduct)

module.exports = routers;
